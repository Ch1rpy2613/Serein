# 前端不再持有和风密钥。请配置 server/.env（见 server/.env.example）。

# Web Push VAPID 公钥（构建期注入；公钥可公开）。
# 生成：cd server && npx web-push generate-vapid-keys
# 私钥 / subject 只放 server/.env（VAPID_PRIVATE_KEY / VAPID_SUBJECT）；此处只填公钥。
VITE_VAPID_PUBLIC_KEY=
