{
  "name": "serein",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "svelte-check --tsconfig ./tsconfig.json && vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "stress:unmount": "node scripts/stress-mount-unmount.mjs",
    "server:dev": "npm run dev --prefix server",
    "server:start": "npm run start --prefix server"
  },
  "dependencies": {
    "maplibre-gl": "^6.1.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/three": "^0.185.3",
    "playwright": "^1.55.0",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.4",
    "typescript": "~5.9",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
